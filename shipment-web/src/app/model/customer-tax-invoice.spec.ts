import { CustomerTaxInvoice } from './customer-tax-invoice';

describe('CustomerTaxInvoice', () => {
  it('should create an instance', () => {
    expect(new CustomerTaxInvoice()).toBeTruthy();
  });
});
